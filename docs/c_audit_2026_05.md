# C Audit Report: Hull

**Date:** 2026-05-11
**Files Scanned:** 108 (66 `.c` + 42 `.h`)
**Issues Found:** 10 (Critical: 2, High: 3, Medium: 3, Low: 2)
**Status:** All issues fixed (2026-05-11)

## Critical Issues

| # | File:Line | Issue | Description |
|---|-----------|-------|-------------|
| C-1 | `src/hull/runtime/lua/mod_worker.c:106-108` | Integer overflow in capacity doubling | `new_cap *= 2` in a `while` loop can wrap to 0 on overflow, causing undersized `realloc` and heap buffer overflow |
| C-2 | `src/hull/runtime/lua/mod_gpu.c:98-100`, `src/hull/runtime/js/mod_gpu.c:92-94` | Path traversal in `gpu.load()` | User-supplied shader name concatenated into filesystem path without validation; `../../../etc/passwd` escapes `shaders/` |

### C-1: Integer overflow in bytecode writer capacity doubling

```c
// src/hull/runtime/lua/mod_worker.c:105-109
if (bw->len + sz > bw->cap) {
    size_t new_cap = bw->cap ? bw->cap * 2 : 4096;
    while (new_cap < bw->len + sz)
        new_cap *= 2;  // <-- wraps to 0 if new_cap >= SIZE_MAX/2+1
    uint8_t *nb = realloc(bw->buf, new_cap);
```

**Fix:** Add overflow guard before the doubling loop:
```c
size_t new_cap = bw->cap ? bw->cap * 2 : 4096;
while (new_cap < bw->len + sz) {
    if (new_cap > SIZE_MAX / 2) return 1;
    new_cap *= 2;
}
```

### C-2: Path traversal in gpu.load()

```c
// src/hull/runtime/lua/mod_gpu.c:97-100
snprintf(path, sizeof(path), "%s/shaders/%s.wgsl",
         lua->base.app_vfs->root_dir, name);
FILE *f = fopen(path, "r");
```

The `name` parameter comes directly from user code (`luaL_checkstring` / `JS_ToCString`) with no validation. A name like `../../etc/passwd` would construct a path outside the app directory. The kernel sandbox (unveil/seatbelt) provides defense-in-depth, but the application layer should reject traversal attempts.

**Fix:** Validate the shader name before path construction:
```c
if (!name[0] || name[0] == '/' || strstr(name, ".."))
    return luaL_error(L, "invalid shader name: %s", name);
```

## High Issues

| # | File:Line | Issue | Description |
|---|-----------|-------|-------------|
| H-1 | `src/hull/migrate.c:201` | Integer overflow in capacity doubling | `capacity *= 2` without overflow check before `realloc` size calculation |
| H-2 | `src/hull/worker_db.c:271-273` | Unchecked `strdup()` for column names | `strdup()` return not checked; NULL propagates to later dereferences |
| H-3 | `src/hull/worker_db.c:145-162` | `memcpy` with potentially NULL source | `sqlite3_column_text()` / `sqlite3_column_blob()` can return NULL for SQL NULL values; `memcpy(dst, NULL, 0)` is undefined behavior |

### H-1: Integer overflow in migration list capacity

```c
// src/hull/migrate.c:200-202
if (ml->count >= capacity) {
    capacity *= 2;  // <-- no SIZE_MAX/2 check
    char **tmp = realloc(ml->names, (size_t)capacity * sizeof(char *));
```

The same pattern is correctly guarded in `src/hull/cap/tool.c:292` with `if (cap > SIZE_MAX / 2)`.

**Fix:**
```c
if (capacity > SIZE_MAX / (2 * sizeof(char *))) {
    migration_list_free(ml);
    closedir(dir);
    return -1;
}
capacity *= 2;
```

### H-2: Unchecked strdup for column names

```c
// src/hull/worker_db.c:271-274
for (int i = 0; i < r->ncols; i++) {
    const char *name = sqlite3_column_name(stmt, i);
    r->col_names[i] = strdup(name ? name : "?");
    // strdup can return NULL — no check
}
```

If `strdup` fails, `r->col_names[i]` is NULL. Later code passes these names to `JS_SetPropertyStr` / `lua_setfield` which will crash on NULL.

**Fix:** Check and fail early:
```c
r->col_names[i] = strdup(name ? name : "?");
if (!r->col_names[i]) {
    op->error = 1;
    snprintf(op->error_msg, sizeof(op->error_msg), "out of memory");
    sqlite3_finalize(stmt);
    return;
}
```

### H-3: memcpy with potentially NULL source pointer

```c
// src/hull/worker_db.c:145-149
const char *text = (const char *)sqlite3_column_text(stmt, col);
size_t len = (size_t)sqlite3_column_bytes(stmt, col);
out->s = malloc(len + 1);
if (out->s) {
    memcpy(out->s, text, len);  // text can be NULL
```

Per C standard, `memcpy(dst, NULL, 0)` is undefined behavior even when `len` is 0. SQLite returns NULL text/blob for NULL column values, but the type check dispatches to `SQLITE_TEXT` / `SQLITE_BLOB` before this code runs, not `SQLITE_NULL`. In practice, SQLite's type affinity means this path is rarely hit with NULL, but it's technically UB.

**Fix:** Guard the memcpy:
```c
if (out->s && text) {
    memcpy(out->s, text, len);
    out->s[len] = '\0';
} else if (out->s) {
    out->s[0] = '\0';
    out->len = 0;
}
```

## Medium Issues

| # | File:Line | Issue | Description |
|---|-----------|-------|-------------|
| M-1 | `src/hull/migrate.c:367-368, 392-393` | Unchecked `strdup()` in loops | `strdup()` return not validated; NULL entries leak prior allocations on cleanup |
| M-2 | `src/hull/agent_lib.c:688` | 64 KB stack buffer | `char buf[65536]` on stack; excessive for constrained environments |
| M-3 | `src/hull/runtime/lua/mod_fs.c:159-296` | Custom path validation bypasses capability layer | Lua `require()` implements its own path normalization instead of routing through `hl_cap_fs_validate()` |

### M-1: Unchecked strdup in migration status

```c
// src/hull/migrate.c:367-368
for (int i = 0; i < count; i++)
    names[i] = strdup(first[i].name + 11);  // no NULL check
```

If any `strdup` fails, `names[i]` is NULL. Later at line 408, `strcmp(names[i], ...)` will crash.

**Fix:** Check each strdup and clean up on failure.

### M-2: Large stack allocation in agent_lib

```c
// src/hull/agent_lib.c:688
char buf[65536];
size_t n = fread(buf, 1, sizeof(buf) - 1, f);
```

This is in the `hull agent errors` command path. A 64 KB stack allocation is safe on typical systems but risky in resource-constrained or deeply-nested contexts. Consider heap allocation.

### M-3: Dual path validation in Lua require

The Lua module loader at `mod_fs.c:159-296` implements custom `normalize_path()` + `strncmp` prefix checking rather than delegating to `hl_cap_fs_validate()` which uses `realpath()` for symlink-aware ancestor verification. This creates two validation implementations that could diverge.

The comment in the code notes the sandbox may block `stat()` on parent directories, which is a valid reason for string-level validation. But it means the Lua require path doesn't get symlink resolution.

## Low Issues

| # | File:Line | Issue | Description |
|---|-----------|-------|-------------|
| L-1 | `src/hull/runtime/js/runtime.c:152` | Overly broad `..` check | `strstr(resolved, "..")` in JS module normalizer rejects valid filenames containing `..` (e.g., `backup..old.js`) |
| L-2 | Multiple files | 4 KB stack buffers | 11 instances of `char buf[4096]` across static.c, migrate.c, app_context.c, tool.c, agent_lib.c — acceptable but worth noting |

## Positive Findings (No Issues)

| Category | Status | Notes |
|----------|--------|-------|
| Unsafe string functions | **Clean** | No `strcpy`, `strcat`, `sprintf`, `gets` anywhere in Hull source |
| Unsafe integer parsing | **Clean** | No `atoi`, `atol`, `atof` — uses `strtol`/`strtof` with validation |
| SQL injection | **Clean** | All SQL uses parameterized binding via `hl_cap_db_*` layer |
| Direct SQLite in runtimes | **Clean** | Only UDF registration code touches `sqlite3_*` directly (appropriate) |
| Shell injection | **Clean** | No `system()`, `popen()`, `exec*()` in runtime bindings |
| Env leakage | **Clean** | All env access through `hl_cap_env_get()` with manifest allowlist |
| Eval exposure | **Clean** | JS `eval()` deleted; Lua `load`/`loadfile`/`dofile` removed |
| Sandbox enforcement | **Clean** | pledge/unveil/seatbelt applied in two phases; no bypass paths |
| Allocator overflow | **Clean** | `src/hull/alloc.c` properly checks `SIZE_MAX / count` before multiply |
| Crypto key zeroing | **Clean** | `hull_secure_zero()` (volatile memset) used for key material |
| fwrite/fread checks | **Clean** | Return values checked in all capability and tool code |
| sqlite3 prepare/finalize | **Clean** | All statements properly paired on all code paths |
| Dead code | **Clean** | No `#if 0` blocks or unreachable code detected |
| Build hardening | **Clean** | `-Wall -Wextra -Wpedantic -Wshadow -Wformat=2 -Werror -fstack-protector-strong` |
| Sanitizer support | **Clean** | ASan+UBSan (`make debug`), MSan (`make msan`), full validation (`make check`) |

## Summary

The Hull codebase demonstrates strong security discipline. The capability layer enforcement is solid — no runtime binding bypasses the `hl_cap_*` boundary for data access. SQL injection is structurally impossible. The sandbox is properly layered (application + kernel).

The findings are concentrated in two areas:
1. **Integer overflow in capacity doubling** (C-1, H-1) — missing `SIZE_MAX/2` guards that are correctly applied elsewhere in the codebase
2. **Unchecked `strdup()` returns** (H-2, M-1) — memory allocation failures propagate as NULL without error handling
3. **Path traversal in `gpu.load()`** (C-2) — user input concatenated into filesystem path without validation

All critical and high issues have straightforward fixes. The codebase is well-structured for auditing — consistent naming, clear capability boundaries, and comprehensive test coverage.

---

# C Audit Report: Hull — Phase D3 Follow-up

**Date:** 2026-05-12
**Scope:** New Phase D3 files (compiler vtable, TCC backend, build_assets) + full rescan of runtime bindings, sandbox, manifest
**Files Scanned:** 28
**Issues Found:** 11 (Critical: 0, High: 1, Medium: 6, Low: 4)
**All Fixed:** Yes — commit `7d4aa11`

## Critical — None

No critical issues found. Capability boundary enforcement remains solid. No SQL injection, no shell injection, no eval/load exposure, no direct FS/SQLite bypass in runtimes.

## High Issues

| # | File:Line | Issue | Fix |
|---|-----------|-------|-----|
| H1 | `runtime/lua/bindings.c:244` | `lua_tolstring` returns NULL if stack top is not a string; NULL passed to `kl_response_body_copy` via `hl_maybe_compress` — UB/crash | Added NULL guard; `luaL_error` on NULL result |

## Medium Issues

| # | File:Line | Issue | Fix |
|---|-----------|-------|-----|
| M1 | `build_assets.c:60,83,91,142` | `char path[1024]` + snprintf with user `dir` — suffix overflow for very long paths; return unchecked | Check snprintf return on all 4 paths; return -1 on truncation |
| M2 | `compiler_tcc.c:39,44` | snprintf return not checked on `tmp_dir`/`tcc_path` construction | Added bounds checks |
| M3 | `main.c:869,871,886` | snprintf return not checked before `mkdir`/`fopen` in agent error-file path | Check returns; skip write on truncation |
| M4 | `runtime/lua/runtime.c:1482` | `ws_route->path[256]` truncated silently; wrong endpoint routed | Check return; `luaL_error` on truncation |
| M5 | `runtime/js/runtime.c:2010` | Same as M4 for JS WebSocket routes | Check return; `log_warn` + skip on truncation |
| M6 | `sandbox.c:106` | No assertion that `SEATBELT_PATH_SIZE >= PATH_MAX` — portability risk | `_Static_assert(SEATBELT_PATH_SIZE >= PATH_MAX, ...)` + `#include <limits.h>` |

## Low Issues

| # | File:Line | Issue | Fix |
|---|-----------|-------|-----|
| L1 | `build_assets.c:146` | `chmod(path, 0755)` return ignored; success returned even if tcc not executable | `if (chmod(...) != 0) return -1` |
| L2 | `sandbox.c:601` | Pledge string second snprintf used `strlen(promises)` offset instead of first snprintf return value | Use `plen` as offset |
| L3 | `runtime/lua/bindings.c:38` | `lua_touserdata` cast without type check; callers guard result but function is inconsistent | Added `lua_isuserdata(L, -1)` guard |
| L4 | `manifest.c:367,376` | `JS_ToCString` result passed to `manifest_strdup` before NULL check | Check `if (s)` before `manifest_strdup` |

## Bug Fix (discovered during audit)

**`compiler_tcc.c`: TCC produces ELF on macOS — not Mach-O**

The embedded TCC binary (AArch64 Darwin, mob branch) produces ELF relocatable objects even on macOS. The macOS linker requires Mach-O, so `hull build` round-trips failed with `ld: unknown file type`. `tcc_is_available()` now returns 0 on `__APPLE__` — TCC remains the default on Linux where both compiler and linker use ELF.

## False Positives Rejected

- **`char tmpl[] = "..."` for mkdtemp** — stack-allocated writable array; correct usage.
- **`argv` bounds in `sys_link`** — `n < HL_COMPILER_MAX_ARGS - 2` checked before increment; NULL sentinel always fits.
- **`ncols <= 0` in `db.c:242`** — already present.
- **Double-free in `crypto.c`** — `free(NULL)` is defined behavior; pattern is correct.
- **`SEATBELT_PATH_SIZE` < `PATH_MAX`** — 4096 ≥ PATH_MAX on all supported platforms (Linux: 4096, macOS: 1024). Converted to assertion (M6).

## Positive Findings

All findings from the 2026-05-11 audit remain fixed. New Phase D3 code adds no new capability boundary issues, no unsafe string functions, no shell injection, no SQL injection paths.
